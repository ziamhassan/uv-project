[project]
name = "typer-task"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=8.4.2",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
